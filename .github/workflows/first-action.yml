name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - Print Greeting
        run: echo "Hello World"
      - Print Goodbye
        run: echo "Done Goodbye"
